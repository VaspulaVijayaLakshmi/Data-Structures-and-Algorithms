class Solution {
public:
    int smallestNumber(int n) {
        
           int x = 1;
    while (x < n)
        x = (x << 1) | 1;  // shift left and add a 1 bit
    return x; 
    }
};

// x = 111 (7)
// x << 1 → 1110
// (x << 1) | 1 → 1111
